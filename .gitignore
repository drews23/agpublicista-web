_templates/
.claude/
.DS_Store
Thumbs.db

# Plantilla externa de consulta, no forma parte del sitio
i-design-with-code/

# Bocetos de logo en iteracion, aun no integrados
lienzo.svg
Artboard*.svg
coding lienzo*.svg
*.ai

# Material fuente para packs descargables: se aloja fuera, nunca en el repo publico
svg/
escenas 3d/

# Skills de HyperFrames (herramienta de autoria de video, no es parte del sitio)
.agents/
skills-lock.json

# Proyectos de video de HyperFrames (fuentes y renders, no son parte del sitio)
videos/

# Identidad del canal de YouTube: se versiona la FUENTE (html, iconos, fuentes
# tipograficas y el script de verificacion), no los PNG renderizados. Pesan
# megas, el repo se despliega entero al hosting y nadie los enlaza desde el
# sitio. Se regeneran con Edge headless (ver assets/canal/LEEME.md).
assets/canal/*.png
