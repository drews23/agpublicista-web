_templates/
.claude/
.DS_Store
Thumbs.db

# Respaldo del historial de chat de la sesion de agente, no es parte del sitio
_historial_chat/

# Plantilla externa de consulta, no forma parte del sitio
i-design-with-code/

# Bocetos de logo en iteracion, aun no integrados
lienzo.svg
Artboard*.svg
coding lienzo*.svg
*.ai

# Material fuente para packs descargables: se aloja fuera, nunca en el repo publico
svg/
escenas 3d/

# Skills de HyperFrames (herramienta de autoria de video, no es parte del sitio)
.agents/
skills-lock.json

# Proyectos de video de HyperFrames (fuentes y renders, no son parte del sitio)
videos/

# Identidad del canal de YouTube: se versiona la FUENTE (html, iconos, fuentes
# tipograficas y el script de verificacion), no los PNG renderizados. Pesan
# megas, el repo se despliega entero al hosting y nadie los enlaza desde el
# sitio. Se regeneran con Edge headless (ver assets/canal/LEEME.md).
assets/canal/*.png

# Material fuente de iconos 3D (Envato): NUNCA al repo ni al deploy
GALERIA DE FAVICONES SVG/

# Paquete del sistema de diseno (skill lienzo-design, generado el 2 sep 2026):
# material de trabajo, no es parte del sitio. La raiz del repo es la raiz web,
# asi que versionarlo lo publicaria en lienzo.tools.
SKILL.md
_ds_README.md
github.md
_adherence.oxlintrc.json
_ds_bundle.js
_ds_manifest.json
thumbnail.html
.thumbnail
styles.css
/css/tool.css
tokens/
components/
guidelines/
templates/
ui_kits/
/assets/logo-lienzo.png

# Fotogramas de video en proceso, tampoco son del sitio
hyperframes/
